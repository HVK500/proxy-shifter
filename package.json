{
  "name": "proxy-shifter",
  "description": "Changes the proxy in a .conf file",
  "version": "0.0.1",
  "scripts": {
    "default": "gulp default"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "gulp": "^3.9.1",
    "mkdirp": "^0.5.1"
  }
}
